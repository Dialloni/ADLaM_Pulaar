# Gando AI — Assets

**Wordmark is the logo.** See `wordmark.html` — Manrope Black, primary→secondary gradient, `background-clip: text`. Also an ADLaM variant is simply the string `𞤘𞤢𞤲𞤣𞤮` set in the same treatment with `font-family: var(--font-adlam)`.

**Icons:** Lucide React in-app. For static HTML, use `https://unpkg.com/lucide@latest` CDN:
```html
<script src="https://unpkg.com/lucide@latest"></script>
<i data-lucide="sparkles"></i>
<script>lucide.createIcons();</script>
```

**No other brand imagery exists upstream.** No photos, no illustrations, no mascots.
