# dependencies
node_modules/

# build output
dist/
build/
coverage/

# environment — NEVER commit API keys
.env
.env.*
!.env.example

# macOS
.DS_Store
.AppleDouble
.LSOverride

# logs
*.log
npm-debug.log*

# editor
.vscode/settings.json
*.suo
*.ntvs*
*.njsproj

# vite cache
.vite/

# Firebase config — contains API keys, never commit
firebase-applet-config.json

# Firebase Admin service account — server secret, never commit
service-account.json
firebase-service-account.json

# Telegram scraper
scraper/*.session
scraper/output/

# Eval harness generated outputs (can be large; contains generated apps)
scripts/eval/results/
